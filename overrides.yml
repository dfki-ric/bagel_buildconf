# This file has the same format than the source.yml files. It can be used to
# locally override version control information.


# currently Bagel needs the develop branch of MARS
overrides:
  - simulation/mars/.*:
      branch: develop
